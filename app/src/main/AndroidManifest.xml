<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kilina_3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_life"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_life_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VitalsInfo"></activity>
        <activity android:name=".InfoPressure" />
        <activity android:name=".StartWindow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>